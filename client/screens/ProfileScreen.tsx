import React from "react";
import { Center, Text } from "native-base";

const ProfileScreen = () => {
  return (
    <Center flex={1}>
      <Text>Profile</Text>
    </Center>
  );
};

export default ProfileScreen;
